name: Test

on:
  push:
    branches:
      - master

jobs:
  process:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls -l
      - run: sh ./scripts/release.sh
